{
  "name": "poc_pt_api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy": "git push heroku master",
    "heroku-postbuild": "cd client && npm install && cd src/shared-components && npm install && cd ../.. && npm run build",
    "logs": "heroku logs --tail",
    "setup": "npm install && cd client && npm install && cd src/shared-components && npm install",
    "start": "NODE_ENV=production node index.js",
    "start:dev": "NODE_ENV=local nodemon index.js"
  },
  "author": "fredieu",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "joi": "^17.3.0",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.11.8"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  }
}
